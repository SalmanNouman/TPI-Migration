using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace VARLab.Example
{
    public class Sample : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            Debug.Log("Hi");
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
